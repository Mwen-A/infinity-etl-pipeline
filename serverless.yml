service: aaa-lambda
plugins:
  - serverless-python-requirements
  - serverless-dotenv-plugin

frameworkVersion: '2'

provider:
  name: aws
  runtime: python3.8
  lambdaHashingVersion: 20201221
  region: eu-west-1
  role: arn:aws:iam::534900169478:role/infinity-cafe-role
  vpc:
    securityGroupIds:
      - sg-00d713bdedcadb4bc
    subnetIds:
      - subnet-0289d4772717bc9cc
      - subnet-0d517b54b1a9e8898

functions:
  execute:
    handler: src/app.execute
    events:
      - s3:
          bucket: infinity-cafe-bucket
          event: s3:ObjectCreated:*
          rules:
            - prefix: incoming/
            - suffix: .csv

package:
  exclude:
    - .venv/**
    - .vscode/**
    - node_modules/**
